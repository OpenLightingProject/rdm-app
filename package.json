{
  "name": "rdm-app",
  "devDependencies": {
    "grunt": "~0.4.5",
    "grunt-bower-task": "^0.4.0",
    "grunt-contrib-uglify": "^0.9.1",
    "karma": "~0.10"
  },
  "scripts": {
    "dependencies": "npm install; grunt bower",
    "prestart": "npm run dependencies",
    "start": "karma start ./unit-test-js/karma.conf.js --no-auto-watch --single-run --reporters=dots --browsers=Firefox"
  }
}
