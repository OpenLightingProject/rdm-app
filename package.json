{
  "name": "rdm-app",
  "devDependencies": {
    "grunt": "~0.4.5",
    "grunt-bower-task": "^0.4.0",
    "grunt-contrib-uglify": "^0.9.1",
    "karma": "~0.10",
    "karma-phantomjs-launcher": "^0.1.4"
  },
  "scripts": {
    "dependencies": "npm install; grunt bower",
    "prefirefox": "npm run dependencies",
    "firefox": "karma start ./unit-test-js/karma.conf.js --no-auto-watch --single-run --reporters=dots --browsers=Firefox",
    "prephantomjs": "npm run dependencies",
    "phantomjs": "karma start ./unit-test-js/karma.conf.js --no-auto-watch --single-run --reporters=dots --browsers=PhantomJS"
  }
}
